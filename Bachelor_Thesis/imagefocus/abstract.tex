\cleardoublepage
\section*{Abstract}

% Three equations
\begin{equation}
E = mc^2
\label{eq:einstein}
\end{equation}

\begin{equation}
a^2 + b^2 = c^2
\label{eq:pythagoras}
\end{equation}

\begin{equation}
e^{i\pi} + 1 = 0
\label{eq:euler}
\end{equation}

% Three references
As shown in Equation \eqref{eq:einstein}, mass and energy are equivalent.

According to Equation \eqref{eq:pythagoras}, a right triangle satisfies the Pythagorean theorem.

Finally, Equation \eqref{eq:euler} is one of the most elegant identities in mathematics.

\begin{lstlisting}[language=Python, caption={Simple loop}, label={lst:loop}]
for i in range(10):
    print(i)
\end{lstlisting}
As shown in Listing~\ref{lst:loop}, the loop prints numbers.

\begin{algorithm}
\caption{Example algorithm}
\label{alg:example}

\For{$i=1$ to $n$}{
    do something\;
}
\end{algorithm}
As shown in Algorithm~\ref{alg:example}, ...

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.6\textwidth]{HHU_Logo_WortBildMarke.png}
    \caption{An example image}
    \label{fig:example}
\end{figure}
As shown in Figure~\ref{fig:example}, the result is clear.

\begin{theorem}\label{thm:main}
    $E=mc^2$
\end{theorem}
As shown in Theorem~\ref{thm:main}, addition is commutative.

\begin{table}[htbp]
\centering
\begin{tabular}{c|c}
A & B \\
\hline
1 & 2
\end{tabular}
\caption{Example table}
\label{tab:example}
\end{table}
I will show you a Table~\ref{tab:example}